import React from "react";
import {Navbar} from "./style";

const NavBar = (props) => {
  // const {image} = props
  return (
    <Navbar>
      <h1>Movie List</h1>
    </Navbar>
  );
}

export default NavBar